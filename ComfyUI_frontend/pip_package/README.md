# ComfyUI Lite Frontend

ComfyUI 轻量化版本前端包。

- 无需 PyTorch 依赖
- 采用节点并发执行机制
- 可通过远程 API 调用 AI 模型
- 适用于轻量级部署与工作流编排场景

## 安装

```bash
pip install comfyui-lite-frontend
```

## 使用

此包提供 `comfyui_frontend_package` 模块，与 ComfyUI 后端兼容。安装后 ComfyUI 会自动识别并使用此前端。
