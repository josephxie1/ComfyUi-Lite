{
  "name": "comfyui-lite-desktop",
  "productName": "ComfyUI Lite",
  "version": "1.0.0",
  "description": "ComfyUI Lite Desktop — 轻量化 AI 工作流编排平台",
  "main": ".vite/build/main.cjs",
  "type": "module",
  "scripts": {
    "dev": "npm run compile && electron .",
    "start": "vite build --config vite.preload.config.ts && vite build --watch --mode startwatch",
    "make": "npm run compile && electron-builder build --config=builder.config.ts",
    "compile": "tsc --noEmit && vite build && vite build --config vite.preload.config.ts && cp src/install.html .vite/build/",
    "make:assets": "node scripts/preMake.js",
    "clean": "rimraf .vite dist assets/ComfyUI"
  },
  "devDependencies": {
    "electron": "31.3.1",
    "electron-builder": "^25.1.8",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "electron-log": "^5.2.0",
    "electron-store": "8.2.0"
  }
}
