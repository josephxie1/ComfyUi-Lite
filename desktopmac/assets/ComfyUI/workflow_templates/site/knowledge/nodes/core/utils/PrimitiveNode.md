# PrimitiveNode

**Category**: utils

## Description

A utility node that provides a raw value output of any type. Used to create constant values that can be connected to other nodes. Commonly used to share a single value across multiple inputs.

**Used in 11 template(s)**
